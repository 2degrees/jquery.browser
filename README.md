jquery.browser
==============

Retrofit `$.browser` back into jQuery after its deprecation


Usage
-----

``` javascript
require(['jquery', 'jquery.browser'], function ($) {
  console.log($.browser);
});
```
